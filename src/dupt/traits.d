module dupt.traits;

import dupt.type;
import std.container.array;

Array!Type getAnnotatedTypes(T)() {
    Array!Type list;
    // auto type = new Type();
    // list.insert(type);
    return list;
}
